#include <stdio.h>

int main(){
    //NULL pointer:

    int x = 100;
    //It's a NULL pointer
    int *p = NULL;
    //It's also a NULL pointer
    int *p = 100;//We are assigning value so it doesn't pointing anyone ...
    return 0;
}
